@page
@model ShoppingFantasy.Pages.CrudPicture.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Picture</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Picture.FileName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Picture.FileName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Picture.PictureUrl)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Picture.PictureUrl)
        </dd>
        <dt class="col-sm-2">
            Photo
        </dt>
        <dd class="col-sm-10">
            <img src="@Model.Picture.PictureUrl" alt="@Model.Picture.FileName" />
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Picture?.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
